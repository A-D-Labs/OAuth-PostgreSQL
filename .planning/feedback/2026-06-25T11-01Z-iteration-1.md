# Feedback — iteration 1 (2026-06-25)

**From:** Diangelo (via cc-bridge, stage-8 feedback)
**Verbatim:** "lets go do tier 2. start again from the beginning of the hl-dev-flow. so restart the phases"

## Interpretation

Tier 1 (CI/CD) shipped. The loop restarts at stage 1 (grill-with-docs), now scoped to **Tier 2 — Security depth** from `robustness-roadmap-PRD.md`:

- **2a. MFA / TOTP** — enrol authenticator via QR, one-time recovery codes, login requires TOTP when enabled, disable after re-auth (stories 6–9).
- **2b. Multi-device sessions** — per-session refresh tokens, list/revoke individual devices, reuse detection scoped per session/family (stories 10–12). NOTE: directly changes the CONTEXT.md domain invariant "one active Refresh Token per User" — needs an ADR + CONTEXT.md update.
- **2c. Access-token revocation + lifecycle caps** — immediate ban/forced-logout via deny-list or server-side check; refresh-token absolute-lifetime cap + idle timeout (stories 13–14).

The grill must lock which of 2a/2b/2c land this iteration (don't assume — Tier 2b is flagged in the PRD as the deepest, highest-risk change). The CI gate built in Tier 1 now protects every Tier-2 change.

## Carry-forward context
- Repo home: `A-D-Labs/OAuth-PostgreSQL` (enforced `dev` CI gate is live).
- Dependency remediation #25 (Spring Boot 3.5.x) still open, independent of Tier 2.
