# Usage

Invoke with phrases like:
- Use the Ralph GitHub TDD skill.
- Give me the Ralph issue loop prompt.
- Provoke the Ralph /tdd GitHub workflow.
- Create a stricter version with full tests.

The output is intended to be pasted into Claude Code running in an environment with GitHub CLI, `/tdd`, Ralph/subagents, and Playwright MCP configured for Chrome.
